spring:
  application:
    name: configserver

  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/sushantpaudel77/my-Banking-application-config-server"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true
